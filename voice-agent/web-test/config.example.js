// Copy this file to config.js and set your Gemini API key and optional backend.
// Get a key at: https://aistudio.google.com/apikey

export const GEMINI_API_KEY = "YOUR_API_KEY_HERE";

// IdeaRefinement backend (registry + conductor prompt). If set, creates a project and applies patches each turn.
export const BACKEND_URL = "";

// Optional fallback URL (e.g. Cloudflare tunnel). Used when BACKEND_URL is unreachable.
export const FALLBACK_URL = "";
